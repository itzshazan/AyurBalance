<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- COARSE only — GPS precision not needed for climate zone detection -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:name=".AyurBalanceApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AyurBalance">

        <!-- Splash Screen — App Entry Point -->
        <activity
            android:name=".ui.splash.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.AyurBalance.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Onboarding Flow -->
        <activity
            android:name=".ui.onboarding.OnboardingActivity"
            android:exported="false"
            android:theme="@style/Theme.AyurBalance.Onboarding" />

        <activity
            android:name=".ui.onboarding.Onboarding2Activity"
            android:exported="false"
            android:theme="@style/Theme.AyurBalance.Onboarding" />

        <activity
            android:name=".ui.onboarding.Onboarding3Activity"
            android:exported="false"
            android:theme="@style/Theme.AyurBalance.Onboarding" />

        <activity
            android:name=".ui.onboarding.OnboardingPrivacyActivity"
            android:exported="false"
            android:theme="@style/Theme.AyurBalance.Onboarding" />

        <!-- Auth Flow -->
        <activity
            android:name=".ui.auth.SignupActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.AyurBalance.Auth" />

        <activity
            android:name=".ui.auth.LoginActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.AyurBalance.Auth">

            <!-- OAuth deep link callback -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="ayurbalance"
                    android:host="login-callback" />
            </intent-filter>

        </activity>

        <activity
            android:name=".ui.auth.OtpVerificationActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.AyurBalance.Auth" />

        <!-- Health Profile Steps -->
        <activity
            android:name=".ui.profile.ProfileStep1Activity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.AyurBalance.Auth" />

        <activity
            android:name=".ui.profile.ProfileStep2Activity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.AyurBalance.Auth" />

        <activity
            android:name=".ui.profile.ProfileStep3Activity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.AyurBalance.Auth" />

        <activity
            android:name=".ui.profile.ProfileStep4Activity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.AyurBalance.Auth" />

        <!-- Location Permission (between Profile Setup and Prakriti) -->
        <activity
            android:name=".ui.location.LocationPermissionActivity"
            android:exported="false"
            android:theme="@style/Theme.AyurBalance.Auth" />

        <!-- Prakriti Assessment -->
        <activity
            android:name=".ui.prakriti.PrakritiQuestionActivity"
            android:exported="false"
            android:theme="@style/Theme.AyurBalance.Auth" />

        <!-- Prakriti Result -->
        <activity
            android:name=".ui.prakriti.PrakritiResultActivity"
            android:exported="false"
            android:theme="@style/Theme.AyurBalance.Auth" />

        <!-- Main Dashboard -->
        <activity
            android:name=".ui.dashboard.DashboardActivity"
            android:exported="false"
            android:theme="@style/Theme.AyurBalance" />

        <!-- Log Food (Camera / ML identification) -->
        <activity
            android:name=".ui.logfood.LogFoodActivity"
            android:exported="false"
            android:theme="@style/Theme.AyurBalance" />

        <!-- Admin Panel -->
        <activity
            android:name=".admin.AdminLoginActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.AyurBalance.Auth" />

        <activity
            android:name=".admin.AdminDashboardActivity"
            android:exported="false"
            android:theme="@style/Theme.AyurBalance" />

    </application>

</manifest>
